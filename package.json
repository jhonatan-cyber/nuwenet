{
  "name": "nuwenet",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/api build && bun --bun run concurrently -k -n api,web \"bun run --cwd apps/api dev\" \"bun run --cwd apps/web dev\"",
    "build": "bun run --cwd apps/api build && bun run --cwd apps/web build",
    "check": "bun run --cwd apps/api check && bun run --cwd apps/web check",
    "start": "bun apps/api/dist/main.js",
    "db:status": "bun tools/db/database.ts status",
    "db:migrate": "bun tools/db/database.ts migrate",
    "db:archive": "bun tools/db/database.ts archive",
    "test": "bun run build && bun test ./test",
    "test:unit": "bun test ./test/unit",
    "preview": "bun run --cwd apps/web build && bun tools/serve-web.ts",
    "test:postgres": "bun run build && bun test ./test/integration/postgres.test.ts",
    "test:ui": "bun run build && bun tools/e2e/smoke-ui.ts",
    "test:dialogs": "bun run build && bun tools/e2e/verify-dialogs.ts",
    "verify": "bun run check && bun run test && bun tools/e2e/smoke-ui.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "concurrently": "^9.2.1",
    "playwright": "^1.63.0"
  },
  "engines": {
    "bun": ">=1.4.0"
  },
  "packageManager": "bun@1.4.0",
  "trustedDependencies": [
    "esbuild"
  ]
}
